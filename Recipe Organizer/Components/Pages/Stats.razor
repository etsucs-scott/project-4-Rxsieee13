@inject RecipeManager RecipeManager

<h2>📊 Ingredient Stats</h2>

<div class="recipe-card">
    <ul>
        @foreach (var s in stats.Take(10))
        {
            <li>@s.Key — used in @s.Value recipes</li>
        }
    </ul>
</div>
